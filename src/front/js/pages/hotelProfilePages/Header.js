import React from "react";

const Header = () => {
    return (
        <header className="header-container">
            <h1 className="header-title">Hotel Profile</h1>
        </header>
    );
};

export default Header;